// This file intentionally defers to src/app/page.tsx which serves the root "/" route.
// The (auth) route group is used only for layout scoping of /login, /cadastro, /recuperar-senha.
export { default } from "@/app/page";
