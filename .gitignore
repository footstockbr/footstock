# Dependencies
node_modules/
motor/node_modules/

# Next.js
.next/
out/

# Environment — TUDO que contém credenciais deve ser ignorado
.env
.env.local
.env.*.local
.env.development
.env.staging
.env.production
.env.docker
.env.deploy
.env.test
.mcp.json
# Backups de .env (sufixos .bak, .backup, .old, timestamps)
.env*.bak*
.env*.backup*
.env*.old
.env.*.bak.*
*.env.bak
!.env.example
!.env.docker.example
!.env.*.example

# Build
dist/
motor/dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/settings.json

# Logs
*.log
npm-debug.log*

# Testing
coverage/
.nyc_output/

# Sentry
.sentryclirc

# Legacy (kept for reference but excluded from new build)

# Credenciais de desenvolvimento
DEV-CREDENTIALS.md

# MCP Sync
.env.deploy
.mcp.json

# Certificados e chaves
*.pem
*.key
*.cert
*.p12
*.pfx

# Arquivos sensíveis de serviços
.railway/
serviceAccountKey.json
firebase-adminsdk-*.json


# Stryker mutation test sandboxes (contêm cópias de .env com credenciais)
.stryker-tmp/

# Load test results (gerados em runtime — não versionar)
tests/load/results/*.json
tests/load/results/*.html
.vercel

# Legacy backup (consolidated into footstock-next)
_*_legacy_backup/

# Pasta de melhorias (ignorada pelo git)
melhorias/

# Backups de banco e exports de dados sensíveis (contêm hashes, PII, auth data)
backups/
*.dump
*.sql.gz
*.sql.bak
db-dump-*
*-dump-*.csv
*-users-*.csv
*-auth-users-*.csv

# Secrets / tokens locais
.npmrc
secrets/
*.secret
*-secrets.json
*-secrets.yaml
*-secrets.yml
